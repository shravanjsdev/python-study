# Set datatype

# Unordered, heterogeneous collection of data
# No duplicate entries are present, it will overwrite
# Cannot be accessed using its index number

set_var = {1, 2, 3, 'One', 'Two', 'Three', 1, 2, 3, 'One', 'Two', 'Three'}
print(set_var)

set_var.add('Six')
print(set_var)
